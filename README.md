# 📄 Интеграция 1С с сервисом «Комбинатор»  
*Текущая реализация (версия 1.0)*

---

## 📌 Содержание
- [Назначение модуля](#-назначение-модуля)
- [Требования](#-требования)
- [Настройка конфигурации](#️-настройка-конфигурации)
- [Сценарии использования](#-сценарии-использования)
- [Ключевые функции](#️-ключевые-функции)
- [Обработка ошибок](#-обработка-ошибок)
- [Особенности и ограничения](#️-особенности-и-ограничения)
- [Примеры кода](#-примеры-кода)

---

## 🎯 Назначение модуля
Модуль обеспечивает интеграцию 1С с сервисом **«Комбинатор»** для:
- Генерации документов по шаблонам на основе данных из 1С
- Работы в двух режимах:
  1. **Локальный режим** — документ возвращается в 1С и сохраняется в присоединенных файлах
  2. **Облачный режим** — документ создается и хранится в сервисе Комбинатор
- Поддержки дополнительных реквизитов и табличных частей объектов 1С
- Автоматического определения типов данных для корректной передачи в сервис

---

## ⚙️ Требования
- **Платформа 1С**: версия не ниже 8.3.20
- **Внешние компоненты**: SSL-библиотека OpenSSL
- **Права доступа**:
  - Чтение/запись констант группы `Кмб_`
  - Права на добавление присоединенных файлов для объектов
  - Сетевой доступ к сервису Комбинатор
- **Серверные настройки**:
  - Для локального сервера: доступ к `192.168.0.40:443`
  - Для облачной версии: доступ к `app.kombinator.ru:443`

---

## 🔧 Настройка конфигурации

### 1. Заполнение констант
| Константа | Назначение | Пример значения | Обязательность |
|-----------|------------|------------------|----------------|
| `Кмб_СтрокаПодключения` | Адрес сервера Комбинатор | `192.168.0.40` (локальный) или `app.kombinator.ru` (облако) | Нет |
| `Кмб_Логин` | Логин для доступа к API | `user@company.ru` | **Да** |
| `Кмб_Пароль` | Пароль для доступа к API | `secure_password` | **Да** |
| `Кмб_СохранятьДокументыВКомбинаторе` | Режим сохранения документов | `Истина`/`Ложь` | **Да** |
| `Кмб_ФорматЗаписиДокумента` | Формат файла при сохранении в 1С | `docx`, `pdf`, `xlsx` | Только при `Кмб_СохранятьДокументыВКомбинаторе = Ложь` |

> **❗ Важно!**  
> При пустом значении `Кмб_СтрокаПодключения` по умолчанию используется `app.kombinator.ru`, **но в текущей реализации адрес всегда перезаписывается на `192.168.0.40`** (локальный сервер). Для работы с облачной версией необходимо отключить эту строку в коде.

### 2. Настройка шаблонов
Для работы с шаблонами необходимо заполнить справочник **«Связь шаблонов с объектами метаданных»**:
- **ИдентификаторКоманды** — уникальный код команды из 1С (например, `КомандаПечатьДоговора`)
- **ИдентификаторШаблона** — ID шаблона в сервисе Комбинатор
- **Наименование** — человекочитаемое название шаблона
- **ИспользуемыеРеквизиты** — таблица соответствия полей шаблона реквизитам 1С
  - `Реквизит` — путь к данным (например, `Контрагент.Наименование`)
  - `КодДопРеквизита` — для дополнительных реквизитов
  - `ТабличнаяЧасть` — признак табличной части
  - `ТабличнаяЧастьИмя` — имя табличной части

---

## 🚀 Сценарии использования

### Сценарий 1: Генерация документа с сохранением в 1С
```bsl
// Параметры вызова
Параметры = Новый Структура;
Параметры.Вставить("ИдентификаторКоманды", "КомандаПечатьДоговора");
Параметры.Вставить("Источник", ЭтаФорма.Объект);

// Вызов генерации
Результат = СгенерироватьДокументПоШаблону(Параметры);

Если Результат.Успех Тогда
    // Результат.ДвоичныеДанныеФайла содержит файл для сохранения
    // Автоматически вызывается ПоместитьФайлВПрисоединенныеФайлы
КонецЕсли;
```

### Сценарий 2: Генерация документа в сервисе Комбинатор
```bsl
// Установить режим сохранения в облаке
Константы.Кмб_СохранятьДокументыВКомбинаторе.Установить(Истина);

Параметры = Новый Структура;
Параметры.Вставить("ИдентификаторКоманды", "КомандаАктВыполненныхРабот");
Параметры.Вставить("Источник", ДокументОснование);

Результат = СгенерироватьДокументПоШаблону(Параметры);

// При успехе пользователь увидит ID документа и ссылку
```

---

## ⚡ Ключевые функции

### `СгенерироватьДокументПоШаблону(СтруктураПараметров) Экспорт`
**Назначение**: Основная точка входа для генерации документов  
**Параметры**:
- `ИдентификаторКоманды` (Строка) — код команды для поиска шаблона
- `Источник` (Ссылка на объект) — объект 1С с данными для генерации

**Возвращает**: Структура с результатами:
- `Успех` (Булево) — успешность операции
- `ДвоичныеДанныеФайла` (ДвоичныеДанные) — файл документа (только для локального режима)
- `НаименованиеШаблона` (Строка) — название использованного шаблона
- `Расширение` (Строка) — формат файла (из константы `Кмб_ФорматЗаписиДокумента`)
- `ИмеетПрисоединенныеФайлы` (Булево) — флаг возможности сохранения в объект

---

### `ПолучитьСоединениеССервисом(Логин, Пароль)`
**Логика подключения**:
1. Берет адрес из константы `Кмб_СтрокаПодключения`
2. Если константа пустая — использует `app.kombinator.ru`
3. **Всегда перезаписывает адрес на `192.168.0.40`** (локальный сервер)
4. Создает SSL-соединение на порту 443
5. Возвращает объект `HTTPСоединение`

> **❗ Особенность текущей реализации**  
> Для работы с облачной версией необходимо закомментировать строку:  
> `СтрокаПодключения = "192.168.0.40";`

---

### `ПолучитьСтруктуруДанныхДокумента(Данные, Шаблон)`
**Назначение**: Формирует данные для передачи в сервис Комбинатор  
**Особенности**:
- Поддерживает вложенность до 4 уровней (например, `Контрагент.Контакты.Телефон.Основной`)
- Автоматически определяет типы данных:
  - Числа, даты, булевы значения передаются «как есть»
  - Остальные типы конвертируются в строки
- Обрабатывает табличные части с динамическим определением колонок
- Работает с дополнительными реквизитами через их код (дополняет код нулями до 4 символов)

---

## ⚠️ Обработка ошибок

### Возможные ошибки и решения
| Ошибка | Причина | Решение |
|--------|---------|---------|
| `Не найден шаблон с идентификатором команды %1` | Отсутствует запись в справочнике шаблонов | Проверьте заполнение справочника `Кмб_СвязьШаблоновСОбъектамиМетаданных` |
| `Не удалось подключиться к сервису` | Нет сетевого доступа к серверу | Проверьте доступность `192.168.0.40:443` или `app.kombinator.ru:443` |
| `При генерации документа произошла ошибка! Статус ответа: 401` | Неверные логин/пароль | Проверьте константы `Кмб_Логин` и `Кмб_Пароль` |
| `Невозможно выполнить команду! Не заданы настройки формата сохранения документов` | Не заполнена константа `Кмб_ФорматЗаписиДокумента` при локальном режиме | Укажите формат файла (pdf/docx/xlsx) |
| `Не удалось получить значение доп. реквизита` | Некорректный код доп. реквизита | Проверьте код в справочнике шаблонов (должен быть 4-значным) |

### Статусы HTTP-ответов
- **200** — Успех
- **401** — Ошибка авторизации
- **404** — Шаблон не найден в сервисе
- **429** — Превышен лимит запросов
- **500** — Внутренняя ошибка сервиса

---

## ⚠️ Особенности и ограничения текущей реализации

### 1. Управление подключением
- **Жестко заданный адрес локального сервера**:
  ```bsl
  СтрокаПодключения = "192.168.0.40"; // Эта строка всегда перезаписывает настройки
  ```
  **Рекомендация**: Для работы с облаком закомментируйте эту строку в коде.

### 2. Безопасность
- Учетные данные хранятся в открытом виде в константах
- **Риск**: Возможность компрометации паролей при доступе к базе
- **Временное решение**: Ограничьте права на чтение констант только для администраторов

### 3. Работа с дополнительными реквизитами
- Код доп. реквизита всегда дополняется нулями до 4 знаков
- **Пример**: Код `12` → `0012`
- **Риск**: Несоответствие реальным кодам в конфигурации

### 4. Производительность
- Отсутствует кэширование шаблонов
- При каждом вызове выполняется полная загрузка данных из справочника
- **Риск**: Замедление работы при большом количестве шаблонов

### 5. Обработка соединений
- Нет гарантированного закрытия HTTP-соединений
- **Риск**: Утечка сетевых ресурсов при частых вызовах

---

## 💡 Примеры кода

### 1. Пример вызова из обработчика команды формы
```bsl
Процедура ПечатьДоговораПоШаблону(Команда)
    
    Если Не ПроверитьНастройкиИнтеграции() Тогда
        Возврат;
    КонецЕсли;
    
    ПараметрыВыполнения = Новый Структура;
    ПараметрыВыполнения.Вставить("ИдентификаторКоманды", "КомандаПечатьДоговора");
    ПараметрыВыполнения.Вставить("Источник", ЭтаФорма.Объект);
    
    Результат = СгенерироватьДокументПоШаблону(ПараметрыВыполнения);
    
    Если Результат.Успех И Результат.ИмеетПрисоединенныеФайлы Тогда
        ПоместитьФайлВПрисоединенныеФайлы(
            ЭтаФорма.Объект,
            Результат.НаименованиеШаблона,
            Результат.ДвоичныеДанныеФайла,
            Результат.Расширение
        );
    КонецЕсли;
    
КонецПроцедуры
```

### 2. Проверка доступности сервиса
```bsl
Функция ПроверитьПодключениеККомбинатору()
    
    Логин = Константы.Кмб_Логин.Получить();
    Пароль = Константы.Кмб_Пароль.Получить();
    ПараметрыОтвета = ПолучитьПараметрыОтвета();
    
    Попытка
        ПроверитьПодключение(Логин, Пароль, ПараметрыОтвета);
        Если ПараметрыОтвета.Статус = 200 Тогда
            Сообщить("Сервис Комбинатор доступен");
            Возврат Истина;
        Иначе
            Сообщить("Сервис недоступен. Код ошибки: " + ПараметрыОтвета.Статус);
        КонецЕсли;
    Исключение
        Сообщить("Ошибка подключения к сервису: " + ОписаниеОшибки());
    КонецПопытки;
    
    Возврат Ложь;
    
КонецФункции
```

---

## 📚 Дополнительная информация
- **Поддерживаемые форматы файлов**: pdf, docx, xlsx
- **Максимальный размер файла**: 25 МБ (ограничение сервиса Комбинатор)
- **Таймаут операций**: 60 секунд (установлено в коде)
- **Версия API**: v1 для авторизации, v2 для генерации документов

> **ℹ️ Примечание**  
> Данная документация актуальна для текущей реализации.  
> В будущих версиях планируется:
> - Устранение хардкода IP-адреса
> - Внедрение безопасного хранения учетных данных
> - Добавление кэширования шаблонов
> - Реализация механизма повторных попыток при ошибках

---

**Дата создания документации**: 20 ноября 2025 г.  
**Версия модуля**: 1.0 (текущая)  
